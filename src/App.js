import { Layout } from 'antd'
import SearchPage from './containers/SearchPage'

const App = () => {
	return (
		<Layout style={{ height: '100vh' }}>
			<SearchPage />
		</Layout>
	)
}

export default App
